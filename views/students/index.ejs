<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>Student List</h1>
  <ul>
    <% students.forEach(student => { %>
      <li>
        <%= student.name %> --- 
        <%= student.isNice ? 'They are a nice person.' : 'They are not a nice person.' %>
      </li>
    <% }) %>
  </ul>
</body>
</html>